# Tenth example: using cowsay cow function to draw a cow saying something.

'''
import cowsay
import sys

if len(sys.argv) == 2:
    #per doc it only accept a string, so we concatenate our string and argument.

    cowsay.cow("hello, " + sys.argv[1])
'''

# Eleventh example: using cowsay trex function to draw a t rex saying something.

'''
import cowsay
import sys

if len(sys.argv) == 2:
    #per doc it only accept a string, so we concatenate our string and argument.

    cowsay.trex("hello, " + sys.argv[1])
'''
